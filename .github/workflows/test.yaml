name: main_workflow

on:
  push:
    branches:
      - test

jobs:
  push-test-result:
    runs-on: [ ubuntu-latest ]
    steps:
      - uses: actions/checkout@main

      - name: build
        run: |
          docker build .
